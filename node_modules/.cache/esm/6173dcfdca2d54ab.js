let tmi;_ca2‍.w("tmi.js",[["default",["tmi"],function(v){tmi=v}]]);
//const tmi = require('tmi.js');

const client = new tmi.Client({
    options: { debug: true },
    identity: {
        username: 'harritvanbeek20',
        password: 'oauth:oauth:xh7uhuuf0ispxfw7ij4s0xqgpl7y5p'
    },
    channels: [ 'harritvanbeek20' ]
});


client.connect().catch(_ca2‍.g.console.error);
client.on('message', (channel, tags, message, self) => {
    if(self) return;
    if(message.toLowerCase() === '!hello') {
        client.say(channel, `@${tags.username}, heya!`);
    }
});