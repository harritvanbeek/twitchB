module.exports = {
    name : "sc",
    execude(client, channel, tags, message){
        client.say(channel, `Ik maak gebruik van het programma : VoiceAttack deze kan je downloaden via stream voor ongeveer 12 € \r\n met een plugin : hcsvoicepacks.com deze zorgt er voor dat je cominicatie terug krijg!`);
    }   
}